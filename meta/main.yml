---

dependencies:
  - vdzhorov.ansible-role-wp-cli

galaxy_info:
  author: vdzhorov
  description: Install Wordpress CLI tool on the target host.
  company: "Delta.BG"
  license: "license (MIT)"
  min_ansible_version: 2.5
  platforms:
    - name: EL
      versions: all
    - name: Debian
      versions: all
    - name: Ubuntu
      versions: all
  galaxy_tags:
    - linux
    - wp-cli
    - wodpress profile
    - wordpress
